<header class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <nav>
    <a class="navbar-brand " href=#>ReviewM</a>
    <ul class="nav navbar-nav navbar-right pull-right">
      <% if logged_in? %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">MENU<b class="caret"></b></a>
          <ul class="dropdown-menu">
            <li><%= link_to "MYPAGE", "/users/#{current_user.id}" %>
            <li><%= link_to "TIMELINE",root_path %>
            <li><%= link_to "POST",new_micropost_path%>　
            <li class="divider"></li>
            <li><%= link_to "LOGOUT",logout_path,method: :delete %>
          </ul>
        </li>
      <% else %>
      <li><%= link_to "LOGIN",login_path %>
      <% end %>
    </nav>
  </div>
</header>
    <div id="microposts-modal" class="modal fade" tabindex="-1" role="dialog" aria-hidden="true"></div>
